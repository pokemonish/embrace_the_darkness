<fest:template xmlns:fest="http://fest.mail.ru"
    context_name="json">
    
    <div class="title title_center">Я есть кекс</div>

    <div class="login input-data">
        <form class="login__form" id="login__form">
            <!-- field -->
            <div class="field">
                <div class="field__in email-field">
                    <input class="input" name="email" type="text" placeholder="Почта" required="" />
                </div>
                <div class="field__error email-field__error">
                </div>
            </div>
            <!-- field -->
            <div class="field">
                <div class="field__in password-field">
                    <input class="input" name="password" type="password" placeholder="Пароль" required="" />
                </div>
                <div class="field__error password-field__error">
                </div>
            </div>
            <!-- field -->
            <div class="field">
                <button class="btn login__btn btn-success field__btn" id="login">Я вхожу</button>
            </div>
        </form>
    </div>
    
    <div class="field">
        <button class="btn btn-primary menu-btn field__btn">Назад</button>
    </div>
    
    
</fest:template>

